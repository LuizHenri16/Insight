image: node:22-alpine

stages:
  - install
  - validate
  - deploy

cache:
  paths:
    - node_modules/
    - .next/cache/

install_dependencies:
  stage: install
  script:
    - npm install
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

validate:
  stage: validate
  script:
    - npm run lint
    - npx tsc --noEmit
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

deploy_vercel:
  stage: deploy
  image: node:22-alpine
  script:
    - npm install --global vercel
    - |
      if [ "$CI_COMMIT_BRANCH" == "main" ]; then
        echo "🚀 Produção oficial: Insight"
        vercel deploy --prod --yes --token=$VERCEL_TOKEN
      else
        echo "🛠️ Preview de teste: Insight"
        vercel deploy --yes --token=$VERCEL_TOKEN
      fi
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"